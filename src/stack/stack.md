对于要用单调递增和单调递减栈的情况：多采用for循环+while循环
因为每一此for循环都要把当前值压入栈中，所以
如果为单调递增栈，要在while循环中把栈中比自己大的元素弹出，这样自己压入栈后，栈中的元素就是比自己小的元素了
如果为单调递减栈，要在while循环中把栈中比自己小的元素弹出，这样自己压入栈后，栈中的元素就是比自己大的元素了
同时，while循环里面要判断栈是否为空。
单调递增栈题目：柱状图中最大矩形面积
单调递减栈题目：每日温度